# sharp uses prebuilt binaries for Linux (Railway/VPS).
# On Windows, if you get a node-gyp error during npm install, run:
#   npm install --ignore-scripts
# then separately:
#   npm install --platform=linux --arch=x64 sharp
# This is only needed for local Windows development.
# On Railway and Linux VPS, npm install works normally.
